<% include partials/header.ejs%>

<h1><%= title %></h1>

<!-- new order form -->
<form method="post">
    <div>
        <label for="firstName">Customer First Name:</label>
        <input name="firstName" id="firstName" required />
    </div>

    <div>
        <label for="lastName">Customer Last Name: </label>
        <input name="lastName" id="lastName" required />
    </div>

    <div>
        <label for="address">Shipping Address: </label>
        <input name="address" id="address" required />
    </div>

    <div>
        <lable for="paymentType">Payment Method: </lable>
        <select name="paymentType" class="dropDown" id="paymentType" required>
            <%
            //set array of payment types and loop through them as user options
            var types = ['Cash', 'Interact Debt', 'MasterCard', 'Visa'];
            for(var i=0; i<types.length; i++){ %>
                <option><%= types[i] %></option>
            <% } %>
        </select>
    </div>

    <h2>Order Details</h2>
    <div>
        <lable for="orderDetail">Item: </lable>
        <select name="orderItem" class="dropDown" id="orderItem" required>
            <%
            //set array of items and loop throught them as user options
            var types = ['Knitted Adult Hat', 'Knitted Child Hat', 'Knitted Scarf'];
            for(var i=0; i<types.length; i++){ %>
                <option><%= types[i] %></option>
            <% } %>
        </select>
    </div>

    <div>
        <lable for="orderColour">Colour:</lable>
        <select name="orderColour" class="dropDown" id="orderColour" required>
            <%
            //set array of colours and loop through them as user options
            var types = ['White', 'Black', 'Brown', 'Grey', 'Orange', 'Navy'];
            for(var i=0; i<types.length; i++){ %>
                <option><%= types[i] %></option>
            <% } %>
        </select>
    </div>
    <div>
        <lable for="orderUnit">Quantity: </lable>
        <select name="orderUnit" class="dropDown" id="orderUnit" required>
            <%
            //set array of quanitity amounts and loop through them as user options
            var types = [ 1, 2, 3, 4, 5];
            for(var i=0; i<types.length; i++){ %>
                <option><%= types[i] %></option>
            <% } %>
        </select>
    </div>

    <div>
        <lable for="orderAddon">Finish: </lable>
        <select name="orderAddon" class="dropDown" id="orderAddon">
            <%
            //set array of item addons and loop through them as user options
            var types = ['None', 'Beaver Fur Pom Pom', 'Raccoon Fur Pom Pom', 'Coyote Fur Pom Pom', 'White Fox Fur Pom Pom', 'Red Fox Fur Pom Pom'];
            for(var i=0; i<types.length; i++){ %>
                <option><%= types[i] %></option>
            <% } %>
        </select>
    </div>

    <div>
        <lable for="orderComment">Comments: </lable>
        <input name="orderComment" id="orderComment" />
    </div>

    <button>Save</button>
</form>

<% include partials/footer.ejs%>